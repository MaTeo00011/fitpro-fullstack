{
  "version": 2,
  "builds": [
    {
      "src": "todoapp/package.json",
      "use": "@vercel/static-build",
      "config": {
        "buildCommand": "cd todoapp && npm run build:vercel",
        "distDir": "dist/todoapp/browser"
      }
    },
    {
      "src": "api/[...path].js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    },
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}